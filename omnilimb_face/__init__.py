"""omnilimb_face — Open-LLM-VTuber capabilities as a hermes-agent plugin.

This package hosts the plugin's backend logic (config, protocol gateway,
voice pipeline, LLM bridge, TTS, expression mapping and session runtime).
The public plugin entry point is :func:`omnilimb_face.plugin.register`.
"""

__version__ = "0.1.0"
